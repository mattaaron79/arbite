# ARBITE.md

_Auto-generated by `arbite init` (arbite 0.1.0) on 2026-08-22. Regenerated every time `arbite init` runs in this directory -- hand edits here will be overwritten on the next `arbite init`; if this content needs to change, edit the package's docs template instead._

## What this is

`arbite` is a low-tech, file-based ticketing system that lives inside this git repo. Tickets are markdown files with YAML frontmatter, stored under `tickets/`, moved between folders as their status changes -- no database, no server. It exists so multiple AI agents (and humans) can pick up tasks, track state, and leave a clean git history of what happened and when.

**Any agent or human working in this repo should use `arbite` for creating, claiming, blocking, shelving, closing, and reopening tasks instead of ad hoc notes or files.** The ticket's folder location is the single source of truth for its state -- an agent's own memory of what it was doing is only a hint, and must be checked against the ticket's actual location before being trusted. Agents should also prefer `arbite note` over directly editing a ticket file to leave progress notes -- it timestamps and attributes the entry consistently instead of relying on freeform hand edits.

## Directory layout

```
tickets/
  open/            actionable, unclaimed
  in_progress/     claimed, being worked
  blocked/         stalled, see blocked_by
  shelved/         shelved, parked for later
  closed/YYYY-MM/  archived by close date
  agents/          one scratchpad file per agent identity
```

Status is represented by folder location, not just a frontmatter field -- `status` mirrors the folder and is kept in sync by every state-changing command in the same operation. Filenames stay stable across moves, so `git log --follow` on a ticket file traces its full lifecycle as folder-move commits.

## Ticket frontmatter fields

- `id` -- unique ticket id, e.g. tic-a1b2 -- generated by `arbite create`, never set by hand
- `title` -- short human-readable summary
- `status` -- open | in_progress | blocked | shelved | closed -- mirrors the ticket's folder, kept in sync by every arbite command
- `type` -- bug | feature | refactor | chore | memo -- memo is produced by 'arbite raw' for requests to update project notes/documentation
- `tier` -- low | medium | high | frontier -- agent capability level required to work the ticket
- `domain` -- what kind of agent/tool this needs, e.g. mesh, image_gen, audio_gen, ui, io -- drives routing
- `epic` -- the larger initiative this ticket belongs to, e.g. mesh-pipeline -- a freeform grouping label for filtering (list/next --epic), not a structural dependency or a ticket id
- `priority` -- numeric urgency index, lower = more urgent (e.g. 1 is highest priority) -- used to order which workable ticket to pick up next; unset (null) sorts last
- `tags` -- freeform list, for human/codebase-area search -- distinct from domain
- `assignee` -- agent id currently working the ticket, e.g. claude.haiku.001, or null if unclaimed
- `depends_on` -- list of other ticket ids that must close first (structural)
- `blocked_by` -- freeform reason OR a ticket id -- why it's stalled, only meaningful when status: blocked
- `created` -- date created (YYYY-MM-DD)
- `updated` -- date of the most recent status change
- `closed` -- date closed (YYYY-MM-DD), null until closed -- drives which closed/YYYY-MM/ folder it archives into

`depends_on` (structural, ticket ids) and `blocked_by` (freeform, human-readable) are deliberately separate fields -- don't collapse them. `tier` (capability level) and `domain` (specialization) are independent axes. `domain` (routing) and `tags` (search) serve different purposes even though both are strings. `priority` (urgency) is independent of `tier` (capability): a trivial chore can still be urgent. `epic` groups tickets under a larger initiative (e.g. 'mesh-pipeline') -- it's a freeform label for filtering, not a structural dependency. When choosing between workable tickets, pick the one with the lowest `priority` number first.

## Agent identity and resuming work

Agent ids follow `company.model.instance`, e.g. `claude.haiku.001`. Each agent has a scratchpad at `tickets/agents/<agent_id>.md` for recording what it's currently working on. To resume across sessions: check your own scratchpad for a last-known ticket id, then verify that ticket is still in `tickets/in_progress/` with `assignee` matching your own id -- the ticket's folder is ground truth, the scratchpad is only a hint. If the scratchpad is absent, stale, or mismatched, fall back to scanning `tickets/in_progress/` for a ticket assigned to you.

## Typical workflow

```
arbite list --status open --domain mesh --tier medium   # find something to work
arbite list next --tier high                            # grab the next workable open ticket
arbite list next --epic mesh-pipeline                   # next workable ticket in an epic
arbite list next --epic classification                  # next raw ticket needing triage
arbite search --params title,body "LOD pop-in"          # find tickets by text
arbite raw feature "add per-mesh LOD"                   # quick capture; classify later
arbite show tic-a1b2                                    # read it in full
arbite claim tic-a1b2 --agent claude.haiku.001          # take it
arbite note tic-a1b2 claude.haiku.001 "progress update" # ...do the work, log progress...
arbite block tic-a1b2 --reason "waiting on tic-c3d4"    # if stalled
arbite shelve tic-a1b2 --reason "parked for later"      # if deprioritized
arbite close tic-a1b2                                   # when done
arbite reopen tic-a1b2                                  # if it turns out not to be done after all
```

A human can also scaffold a ticket to fill in by hand later with `arbite create --blank` (optionally combined with any of --title/--type/--tier/--domain for whatever's already known). It fills in only the structural fields and leaves the rest as TODO placeholders, with a body warning telling agents not to claim it until it's been completed and saved.

A **raw** ticket (`arbite raw <memo|feature|bug> <message>`) is a deliberately unclassified quick capture of a brief request: it sets only the `type` and a placeholder title (`<type> (raw): Requires Classification`), auto-groups the ticket under the `classification` epic (so triage/classification jobs can discover it with `arbite list next --epic classification`), and its body lists what still needs to be filled in -- a real title, `tier`, `domain`, a real `epic`, `priority`, and an expanded description based on the original request -- before the ticket can be claimed or worked. Raw tickets exist so a thought isn't lost, not as workable tasks: treat them as requiring triage, and classify them before picking them up. A `memo` raw ticket is primarily a request to update any project notes / documentation that is being maintained, rather than a code change.

## Full command reference

Rendered directly from `arbite --help` and each subcommand's `--help` for the installed version, so it always matches the actual CLI. Every command also supports `-h`/`--help` directly if this file is ever out of date.

```
usage: arbite [-h] [--version] command ...

File-based ticketing system

positional arguments:
  command
    init      create tickets/ folder structure and agent scratchpads
    create    create a new ticket in open/
    raw       create an unclassified raw ticket in open/ from a brief request
    list      list/filter tickets
    search    search tickets by field and/or body text
    claim     move a ticket to in_progress/ and assign it
    block     move a ticket to blocked/
    close     move a ticket to closed/YYYY-MM/
    reopen    move a ticket back to open/ (reopen it)
    shelve    move a ticket to shelved/ (shelve it)
    note      append a timestamped, agent-identified note to a ticket
    show      print a ticket's full contents
    deps      walk depends_on to show a dependency tree
    depend    add a dependency to a ticket's depends_on, or clear all of its
              dependencies
    set       set one or more ticket properties

options:
  -h, --help  show this help message and exit
  --version   show program's version number and exit
```

### `arbite init`

```
usage: arbite init [-h]

Create tickets/{open,in_progress,blocked,closed,agents}/ in the current
directory (like 'git init'), and pre-create a scratchpad file under
tickets/agents/ for every id listed in an 'agents:' list in ./arbite.yaml, if
present.

options:
  -h, --help  show this help message and exit
```

### `arbite create`

```
usage: arbite create [-h] [--title TITLE]
                     [--type {bug,feature,refactor,chore}]
                     [--tier {low,medium,high}] [--domain DOMAIN]
                     [--epic EPIC] [--priority PRIORITY] [--tags TAGS]
                     [--depends-on DEPENDS_ON] [--description DESCRIPTION]
                     [--blank]

Create a new ticket in tickets/open/ with a generated id (tic-XXXX).
--title/--type/--tier/--domain are required unless --blank is given.

options:
  -h, --help            show this help message and exit
  --title TITLE         short ticket title (required unless --blank)
  --type {bug,feature,refactor,chore}
                        kind of work (required unless --blank)
  --tier {low,medium,high}
                        capability level required to work this ticket
                        (required unless --blank)
  --domain DOMAIN       what kind of agent/tool this needs, e.g. mesh,
                        image_gen, audio_gen, ui, io (required unless --blank)
  --epic EPIC           larger initiative this ticket belongs to, e.g. 'mesh-
                        pipeline' (optional; group tickets under an epic with
                        `arbite list --epic` / `arbite list next --epic`)
  --priority PRIORITY   numeric urgency index, lower = more urgent (e.g. 1 is
                        highest priority); used to decide which workable
                        ticket to pick up first
  --tags TAGS           comma-separated, freeform, for codebase-area search,
                        e.g. 'normals,curves'
  --depends-on DEPENDS_ON
                        comma-separated ticket ids that must close before this
                        one, e.g. 'tic-a1b2,tic-c3d4'
  --description DESCRIPTION
                        free-text body under the '## Description' heading
  --blank               scaffold a blank template ticket for a human to fill
                        in by hand: fills only the structural fields
                        (id/status/created/updated), leaves
                        --title/--type/--tier/--domain as TODO placeholders if
                        not given, and adds a body warning telling agents not
                        to claim it until it's been filled in and saved
```

### `arbite raw`

```
usage: arbite raw [-h] TYPE MESSAGE [MESSAGE ...]

Capture a brief request as a raw ticket in tickets/open/. Sets the type and
title to '<type> (raw): Requires Classification', leaves tier/domain/priority
as TODO placeholders, auto-groups the ticket under the 'classification' epic
(so triage/classification jobs can find it with `arbite list next --epic
classification`), and writes a body explaining that the ticket must be filled
out (a real title, tier, domain, epic, priority, and an expanded description)
before it can be claimed or worked. Use 'memo' when the request is to update
project notes / documentation rather than make a code change.

positional arguments:
  TYPE        kind of raw ticket: memo | feature | bug
  MESSAGE     brief request to capture, e.g. "users can't save without auth"
              (joined with spaces if multiple words)

options:
  -h, --help  show this help message and exit
```

### `arbite list`

```
usage: arbite list [-h] [--status {open,in_progress,blocked,shelved,closed}]
                   [--tier {low,medium,high}] [--domain DOMAIN] [--epic EPIC]
                   [--priority PRIORITY] [--assignee ASSIGNEE]
                   [--tic TICKET_ID] [--tree | --topo]
                   [SUBCOMMAND]

List tickets, optionally filtered by one or more fields. Sorted so that within
a status, more urgent tickets (lower priority number) come first.

positional arguments:
  SUBCOMMAND            'next' prints a single list entry for the next open
                        ticket in topological dependency order (most urgent
                        first); combine with --tier/--epic to restrict to a
                        capability tier or an epic

options:
  -h, --help            show this help message and exit
  --status {open,in_progress,blocked,shelved,closed}
                        filter by status
  --tier {low,medium,high}
                        filter by tier
  --domain DOMAIN       filter by domain
  --epic EPIC           filter by epic (the larger initiative a ticket belongs
                        to), e.g. 'mesh-pipeline'
  --priority PRIORITY   filter by exact priority number (lower = more urgent)
  --assignee ASSIGNEE   filter by assignee agent id
  --tic TICKET_ID       filter by comma-separated ticket ids, each a wildcard
                        (substring) match, e.g. 'f6' finds tic-f607; combine
                        with --tree/--topo to root the dependency view at the
                        matching tickets and pull in every transitive
                        dependency beneath them
  --tree                render the list as a dependency tree (children =
                        depends_on) instead of a flat table; siblings sorted
                        by priority
  --topo                render the list as a vertical list in topological
                        dependency order; when several tickets are ready at
                        once, the most urgent (lowest priority number) comes
                        first
```

### `arbite search`

```
usage: arbite search [-h] [--params FIELDS]
                     [--status {open,in_progress,blocked,shelved,closed}]
                     [-r | -w]
                     SEARCH_TEXT [SEARCH_TEXT ...]

Search every ticket for SEARCH_TEXT, optionally restricted to specific fields
with --params. Matching is a case-insensitive substring by default; -w adds
simple wildcards ('*' matches any text, e.g. '*popup*') and -r treats
SEARCH_TEXT as a regular expression. A ticket matches if any selected field
matches.

positional arguments:
  SEARCH_TEXT           text to search for (joined with spaces if multiple
                        words)

options:
  -h, --help            show this help message and exit
  --params FIELDS       comma-separated ticket fields to search, e.g.
                        'title,body'; 'body' means the rest of the ticket
                        (markdown body), 'all' means every field plus the body
                        (default: all)
  --status {open,in_progress,blocked,shelved,closed}
                        only search tickets with this status (default: all
                        statuses)
  -r, --regex           treat SEARCH_TEXT as a regular expression (case-
                        insensitive)
  -w, --wildcard        simple wildcards: '*' matches any text, e.g. '*popup*'
                        (case-insensitive)
```

### `arbite claim`

```
usage: arbite claim [-h] --agent AGENT TICKET_ID

Move a ticket to tickets/in_progress/, set its assignee, and update
status/updated.

positional arguments:
  TICKET_ID      ticket id or any wildcard (substring) match, e.g. 'f6' or
                 'tic-f607' both resolve to tic-f607; if several tickets
                 match, the first alphabetically is used

options:
  -h, --help     show this help message and exit
  --agent AGENT  agent id claiming the ticket, e.g. claude.haiku.001
                 (required)
```

### `arbite block`

```
usage: arbite block [-h] --reason REASON TICKET_ID

Move a ticket to tickets/blocked/, set blocked_by, and update status/updated.

positional arguments:
  TICKET_ID        ticket id or any wildcard (substring) match, e.g. 'f6' or
                   'tic-f607' both resolve to tic-f607; if several tickets
                   match, the first alphabetically is used

options:
  -h, --help       show this help message and exit
  --reason REASON  why it's stalled: freeform text or another ticket id
                   (required)
```

### `arbite close`

```
usage: arbite close [-h] TICKET_ID

Move a ticket to tickets/closed/YYYY-MM/ (by today's date) and set
status/closed/updated.

positional arguments:
  TICKET_ID   ticket id or any wildcard (substring) match, e.g. 'f6' or
              'tic-f607' both resolve to tic-f607; if several tickets match,
              the first alphabetically is used

options:
  -h, --help  show this help message and exit
```

### `arbite reopen`

```
usage: arbite reopen [-h] [--agent AGENT] TICKET_ID

Move a ticket that is not currently open back to tickets/open/: clear its
closed date and block reason, append an automatic 'Reopened' note, and update
status/updated.

positional arguments:
  TICKET_ID      ticket id or any wildcard (substring) match, e.g. 'f6' or
                 'tic-f607' both resolve to tic-f607; if several tickets
                 match, the first alphabetically is used

options:
  -h, --help     show this help message and exit
  --agent AGENT  agent id (or 'system') attributed on the automatic reopen
                 note, e.g. claude.haiku.001 (default: system)
```

### `arbite shelve`

```
usage: arbite shelve [-h] [--reason REASON] TICKET_ID

Move a ticket to tickets/shelved/, set status/updated, and append an automatic
timestamped note recording that it was shelved (including --reason if given).

positional arguments:
  TICKET_ID        ticket id or any wildcard (substring) match, e.g. 'f6' or
                   'tic-f607' both resolve to tic-f607; if several tickets
                   match, the first alphabetically is used

options:
  -h, --help       show this help message and exit
  --reason REASON  why it's being shelved; included in the automatic note
                   (optional)
```

### `arbite note`

```
usage: arbite note [-h] TICKET_ID AGENT_ID MESSAGE [MESSAGE ...]

Append a timestamped, agent-identified entry to a ticket's '## Notes' section
(blank line between entries) and update 'updated'. Agents should prefer this
over directly editing a ticket file to leave progress notes.

positional arguments:
  TICKET_ID   ticket id or any wildcard (substring) match, e.g. 'f6' or
              'tic-f607' both resolve to tic-f607; if several tickets match,
              the first alphabetically is used
  AGENT_ID    agent id leaving the note, e.g. claude.haiku.001
  MESSAGE     note text (joined with spaces if multiple words)

options:
  -h, --help  show this help message and exit
```

### `arbite show`

```
usage: arbite show [-h] TICKET_ID

Print a ticket's raw markdown file (frontmatter + body).

positional arguments:
  TICKET_ID   ticket id or any wildcard (substring) match, e.g. 'f6' or
              'tic-f607' both resolve to tic-f607; if several tickets match,
              the first alphabetically is used

options:
  -h, --help  show this help message and exit
```

### `arbite deps`

```
usage: arbite deps [-h] TICKET_ID

Recursively walk a ticket's depends_on field and print the dependency tree.

positional arguments:
  TICKET_ID   ticket id or any wildcard (substring) match, e.g. 'f6' or
              'tic-f607' both resolve to tic-f607; if several tickets match,
              the first alphabetically is used

options:
  -h, --help  show this help message and exit
```

### `arbite depend`

```
usage: arbite depend [-h] TIC_A [TIC_B]

Declare that one ticket depends on another. With two arguments, <tic_a> is
made to depend on <tic_b>: <tic_b> is added to <tic_a>'s depends_on
(deduplicated, existing dependencies are kept). With a single argument, all of
<tic_a>'s dependencies are cleared.

positional arguments:
  TIC_A       ticket id or any wildcard (substring) match, e.g. 'f6' or
              'tic-f607' both resolve to tic-f607; if several tickets match,
              the first alphabetically is used
  TIC_B       ticket that <TIC_A> depends on; omit to clear all of <TIC_A>'s
              dependencies

options:
  -h, --help  show this help message and exit
```

### `arbite set`

```
usage: arbite set [-h] TICKET_ID PROPERTY VALUE [PROPERTY VALUE ...]

Set one or more ticket properties on an existing ticket. Properties are given
as PROPERTY VALUE pairs and any number can be set in one call; quote any value
that spans more than one word. Type-aware: 'tags' and 'depends_on' are comma-
separated lists, 'priority' must be an integer, and an empty quoted value ('')
clears a field. If 'status' is set, the ticket is moved to the matching folder
so folder and frontmatter never disagree (moving to 'closed' auto-dates
'closed'). 'id' is structural and cannot be set.

positional arguments:
  TICKET_ID       ticket id or any wildcard (substring) match, e.g. 'f6' or
                  'tic-f607' both resolve to tic-f607; if several tickets
                  match, the first alphabetically is used
  PROPERTY VALUE  one or more PROPERTY VALUE pairs to set, e.g. title 'New
                  Title' tier high; quote any value that spans more than one
                  word, and use an empty quoted value ('') to clear a field

options:
  -h, --help      show this help message and exit
```

