---
id: tic-0088
title: Add 'arbite submit' -- send a ticket to review, or close it
status: closed
type: feature
tier: medium
domain: cli
epic: workflow
priority: 3
tags:
- cli
- review
- workflow
assignee: null
depends_on:
- tic-8c96
- tic-b0ac
- tic-f177
blocked_by: null
created: '2026-09-20T12:25:55'
updated: '2026-09-20T14:23:15'
closed: '2026-09-20T14:23:15'
---

## Description
Add 'arbite submit <id>' for an agent that has finished the work and is handing it off.

Behaviour depends on the project config's 'review' flag:
- review: true (the default) -- status becomes 'review', the ticket moves to review/, and the assignee is KEPT. A ticket in review is still owned by whoever did the work; they are who a reviewer sends it back to.
- review: false -- the ticket is closed exactly as 'arbite close' does, including dating 'closed'.

Both paths append an auto-note ('Submitted for review.' / 'Submitted; closed (review disabled).'), with an optional --message appended as detail, following the pattern release/shelve/unblock already use.

Use the same compare-and-swap discipline as claim: read, build the Expect, write, so two agents racing cannot both submit.

Refuse to submit a ticket that is already closed, and say so clearly.

Acceptance: with review enabled a submitted ticket is in review/ with its assignee intact and a note; with review disabled the same command closes it.

## Notes
- 2026-09-20T14:23:15 zoo.orch.001: Added "arbite submit": reads the committed review flag through config.review_enabled() -- with review on (default) the ticket becomes review and moves to review/ keeping its assignee, since that agent is who a reviewer sends it back to; with review: false the same command closes it, dated exactly as arbite close dates it. Both paths append the specified auto-note with --message as detail, in the release/shelve/unblock shape, and a ticket that is already closed is refused. Uses the same read/Expect/update compare-and-swap as claim. review_enabled() docs updated now that submit is its only reader; guide and README updated.
